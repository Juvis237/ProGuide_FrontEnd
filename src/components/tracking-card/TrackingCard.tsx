import React from 'react'

interface Props{
    title: string
    id: string
    status: string
}

const TrackingCard = ({title, id, status}) => {
    return <div></div>
}

export default TrackingCard
